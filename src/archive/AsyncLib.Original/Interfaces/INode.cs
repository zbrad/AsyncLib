using System;

namespace ZBrad.AsyncLib.Nodes
{
    // working terms:
    //   nodes and values both contain a supplied typed value
    //   nodes are equatable
    //   values are comparable
    //

    //public interface INode<T> : ILink, IEquatable<INode<T>> where T : IEquatable<T>
    //{
    //    long Id { get; }
    //    T Item { get; }
    //}

    //public interface IValue<T> : ILink, IEquatable<IValue<T>>, IComparable<IValue<T>> where T : IEquatable<T>, IComparable<T>
    //{
    //    long Id { get; }
    //    T Item { get; }
    //}
}